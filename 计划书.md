# TuneLab Agent 多接口支持计划书

## 背景与结论

本计划基于 CodeGraph 对 TuneLab 与本仓库内 `cherry-studio` 源码的分析。

TuneLab 当前 Agent 只有一个内置引擎：

- `TuneLab/Extensions/Agent/AgentModelManager.cs` 只注册 `openai-compatible`。
- `TuneLab/Agent/Models/OpenAICompatibleEngine.cs` 的设置项是 `base_url`、`api_key`、`model` 手动输入。
- `TuneLab/Agent/Models/OpenAICompatibleSession.cs` 只调用 OpenAI-compatible `/chat/completions`，且 `SupportedInput` 固定返回 `Text | Image`，没有按模型真实能力判断。
- 设置面板 `PropertyObjectController` 当前支持静态 `TextBoxConfig`、`ComboBoxConfig`、`SliderConfig` 等声明式控件，但没有“刷新模型列表”“打开 API Key 页面”“异步加载选项”这类交互控件。

Cherry Studio 的 provider 数据主要来自：

- `cherry-studio/packages/provider-registry/data/providers.json`
- `cherry-studio/packages/provider-registry/data/provider-models.json`
- `cherry-studio/packages/provider-registry/data/models.json`

这些数据包含 provider id、名称、默认 chat endpoint、endpoint baseUrl、adapter family、models API URL、鉴权方式、模型能力等信息。本计划只接入“可以通过 Base URL 配置访问”的 provider，包括 OpenAI-compatible、Anthropic Messages、Gemini Generate Content 这三类可用 Base URL 调用的 endpoint；`cherryin` 以及 OAuth/CLI、云厂商专有鉴权、无通用 Base URL 的 provider 均不迁入 TuneLab Agent provider 列表。

## 目标

1. TuneLab Agent 支持 Cherry Studio 中可通过 Base URL 配置访问的 provider，排除 `cherryin`。
2. 选择各大厂/provider 后，自动填入对应 API 地址。
3. 模型选择不再手动输入，改为从 API 获取并用下拉/列表选择。
4. 根据模型信息判断是否多模态，准确控制附件按钮与请求内容。
5. 设置界面提供直达 API Key 获取页面的入口。
6. 保留自定义 Base URL provider 能力，避免只支持预设厂商；自定义 provider 默认按 OpenAI-compatible 协议访问。

## 支持范围

Cherry Studio provider 不全是同一种协议。本计划只纳入存在可配置 `baseUrl`、且 TuneLab 可以用普通 HTTP API Key 或无 key 方式访问的 provider。判断标准：

- provider 的 endpoint config 中存在可用 `baseUrl`。
- endpoint family 属于 TuneLab 本计划支持的三类：`openai-chat-completions`、`anthropic-messages`、`google-generate-content`。
- 鉴权方式是 API Key、无 key、本地服务，或与 API Key 等价的 Bearer Token。
- 不依赖 OAuth 登录、外部 CLI 登录、云厂商签名、service account、region/project/deployment 等专有配置。

纳入支持：

- OpenAI-compatible / OpenAI-style Base URL provider，例如 `deepseek`、`openrouter`、`silicon`、`moonshot`、`zhipu`、`dashscope`、`doubao`、`groq`、`mistral`、`together`、`fireworks`、`nvidia`、`perplexity`、`modelscope` 等。
- Anthropic Messages Base URL provider，例如 `anthropic`，以及 Cherry Studio catalog 中带 `anthropic-messages.baseUrl` 的厂商 endpoint。
- Gemini Generate Content Base URL provider，例如 `gemini`，以及 Cherry Studio catalog 中带 `google-generate-content.baseUrl` 的厂商 endpoint。
- 本地 Base URL provider，例如 `ollama`、`lmstudio`、`gpustack`、`ovms`。
- 聚合/中转服务，例如 `new-api`、`aionly`、`302ai`、`aihubmix`、`dmxapi`、`ppio`、`tokenhub` 等，只要 catalog 中能解析出可用 Base URL。
- 自定义 provider：用户手动输入 `base_url`，按 OpenAI-compatible 协议访问。

不纳入本计划：

- `cherryin`。
- OAuth/CLI provider：`openai-codex`、`grok-cli`、`claude-code`。
- 云厂商专有配置 provider：`azure-openai`、`aws-bedrock`、`vertexai`。
- 没有可用 Base URL、或需要非 Bearer/API Key 鉴权签名的 provider。
- Anthropic/Gemini endpoint 如果没有可配置 Base URL，或需要 service account / project / region 等额外云厂商配置，则不纳入。

## Provider Catalog 设计

新增内部 catalog，不进入 `TuneLab.SDK` / `TuneLab.Foundation` 公共 ABI。

建议新增文件：

- `TuneLab/Agent/Providers/AgentProviderCatalog.cs`
- `TuneLab/Agent/Providers/AgentProviderDefinition.cs`
- `TuneLab/Agent/Providers/AgentEndpointDefinition.cs`
- `TuneLab/Agent/Providers/AgentModelInfo.cs`

核心字段：

- `ProviderId`
- `DisplayName`
- `DefaultEndpointType`
- `EndpointConfigs`
- `DefaultBaseUrl`
- `AdapterFamily`
- `EndpointFamily`
- `ModelsApiUrl`
- `ApiKeyUrl`
- `NoApiKeyRequired`
- `ModelsApiDisabled`
- `IsBaseUrlConfigurable`

数据来源策略：

1. 从 Cherry Studio `providers.json` 生成一份 C# 静态 catalog，排除 `cherryin`。
2. 只导入存在可用 `baseUrl`、且 endpoint family 属于 OpenAI-compatible / Anthropic Messages / Gemini Generate Content 的 endpoint；没有 Base URL 的 provider 不进入下拉列表。
3. 不复制 Cherry Studio 的完整复杂配置，不导入 OAuth/CLI/云厂商专有鉴权字段。
4. 后续可写一个小工具从 Cherry Studio JSON 重新生成 catalog，降低手动同步成本。

## 设置界面设计

现有 `OpenAICompatibleEngine.GetPropertyConfig` 应从“单接口手填”改成“provider 驱动配置”。实现上建议保留当前引擎 id 以兼容已保存设置，但内部按 `EndpointFamily` 分发到不同 session。

建议字段：

- `provider_id`：下拉选择可配置 Base URL 的 provider，默认 `openai-compatible-custom` 或一个稳定可用的预设 provider。
- `endpoint_family`：由 provider 自动带出，通常不暴露给用户；同一 provider 有多个 endpoint 时可在高级模式中选择 OpenAI-compatible / Anthropic / Gemini。
- `base_url`：根据 provider 自动填入；允许用户覆盖。
- `api_key`：密码框；本地/no-key provider 可选。
- `api_key_url`：直达入口。由于当前没有按钮控件，需要新增宿主 UI 能力。
- `model`：改为模型下拉，不再要求用户手动输入。
- `model_is_multimodal`：内部字段或只读状态，由模型同步结果写入。
- `temperature`
- `max_tokens`

需要新增的 UI 能力：

- `ButtonConfig` 或 `ActionConfig`：用于“获取 API Key”“刷新模型”。
- `DynamicComboBoxConfig` 或 provider 设置页内专用模型选择控件：支持异步加载、loading、错误提示、空列表。
- 打开外部链接的宿主服务，例如 `ExternalUrl.Open(url)`，避免 agent 代码直接散落平台调用。

如果短期不想扩展通用 `ControllerConfig`，可以在 `AgentSideBarContentProvider` 的设置页为 Agent 自己手写 provider/model 区块，只把温度等通用字段继续交给 `PropertyObjectController`。

## 模型列表获取

新增 `AgentModelDiscoveryService`，负责按 provider 的 Base URL 拉取模型。

OpenAI-compatible 默认：

- URL：`{baseUrl.TrimEnd('/')}/models`
- Header：`Authorization: Bearer {apiKey}`
- 解析：`data[].id`

Anthropic Messages：

- 默认 URL：`{baseUrl.TrimEnd('/')}/v1/models`，如果 catalog 中提供更明确的 models API URL 则优先使用 catalog。
- Header：`x-api-key: {apiKey}`，并发送 Anthropic 版本 header。
- 解析：优先读取 `data[].id`。

Gemini Generate Content：

- 默认 URL：`{baseUrl.TrimEnd('/')}/v1beta/models?key={apiKey}` 或按 catalog 中的 baseUrl 规范拼接。
- 解析：读取 `models[].name`，模型 id 统一去掉 `models/` 前缀后存入 TuneLab。
- 请求生成内容时再按 Gemini API 要求还原成 `models/{model}:generateContent` 路径。

OpenRouter：

- 优先使用 Cherry Studio catalog 中的 `modelsApiUrls.default`，如 `https://openrouter.ai/api/v1/models`。
- 可读取 `architecture.input_modalities` / `architecture.modality` / `supported_parameters` 等字段判断视觉能力。

Ollama / 本地服务：

- 优先请求 `{baseUrl}/api/tags`。
- 兼容 OpenAI 模式时可回退 `{baseUrl}/v1/models` 或 `{baseUrl}/models`，实际路径需按当前 baseUrl 规范处理。

不支持模型拉取的 Base URL provider：

- 如果 Cherry Studio catalog 标记 `modelsApiDisabled`，或请求失败但 provider 仍可连接，UI 显示错误并允许保留上次选中模型。
- 不恢复“纯手动输入模型名”作为主流程；仅可考虑隐藏的高级兜底，避免破坏“模型从 API 获取”的目标。

缓存策略：

- 按 `provider_id + base_url + api_key hash` 缓存模型列表。
- 设置页打开时可懒加载；用户点击刷新强制重新请求。
- 请求失败时保留上次成功列表，并显示错误，不清空已选模型。

## 多模态判断

判断优先级：

1. API 返回的模型能力字段，例如 OpenRouter 的 input modalities。
2. Cherry Studio `models.json` / `provider-models.json` 中的 capability/meta。
3. 内置启发式兜底：名称包含 `vision`、`vl`、`omni`、`gpt-4o`、`qwen-vl` 等可标记为可能支持图片。
4. 无可靠信息时默认只启用 `Text`，避免把图片发给不支持图片的模型。

运行时改动：

- `OpenAICompatibleSession` 构造参数新增 `AgentModality supportedInput`。
- `SupportedInput` 不再硬编码 `Text | Image`。
- 创建 session 时从选中模型的能力信息传入。
- 新增 `AnthropicMessagesSession` 和 `GeminiGenerateContentSession`，把 TuneLab 的中立消息/图片附件格式分别转换为 Anthropic content blocks 与 Gemini parts。
- 发送图片前仍需保留服务端错误处理，因为 provider 返回的能力并非总是准确。

## API Key 入口

Cherry Studio 当前 `providers.json` 中很多 `apiKeyUrl` 为空，因此 TuneLab 需要维护一份补充表。

建议新增：

- `AgentProviderKeyLinks.cs`

策略：

- 对 Base URL provider 中的常见厂商提供官方 console/API key 页面，例如 OpenAI、Anthropic、Gemini、DeepSeek、OpenRouter、硅基流动、阿里百炼、火山方舟、月之暗面、智谱、Groq、Mistral、Together、Fireworks、NVIDIA、GitHub Models 等。
- 对没有明确 API Key 页面或本地服务 provider，显示“打开官网”或隐藏按钮。
- 所有链接应集中维护，避免 UI 代码散落 URL。

## 实施步骤

1. 生成 provider catalog
   - 从 Cherry Studio `providers.json` 导入 provider，排除 `cherryin`。
   - 只保留存在可用 `baseUrl` 且不依赖 OAuth/CLI/云厂商专有鉴权的 provider。
   - 按 endpoint family 标记 OpenAI-compatible、Anthropic Messages、Gemini Generate Content。
   - 为大厂补充 API Key 链接。

2. 扩展设置 UI
   - 增加 provider 下拉。
   - provider 变更时自动写入默认 `base_url`。
   - 增加 API Key 获取入口。
   - 增加模型刷新与模型选择控件。

3. 新增模型发现服务
   - 实现 OpenAI-compatible `/models`。
   - 实现 Anthropic `/v1/models`。
   - 实现 Gemini models list。
   - 实现 OpenRouter models API 特化解析。
   - 实现 Ollama `/api/tags`。
   - 加入缓存、取消、错误显示。

4. 改造 session 创建
   - `OpenAICompatibleEngine.CreateSession` 读取 `provider_id/endpoint_family/base_url/api_key/model/model_is_multimodal`。
   - `OpenAICompatibleSession` 接收真实 `SupportedInput`。
   - 按 `endpoint_family` 创建 `OpenAICompatibleSession`、`AnthropicMessagesSession` 或 `GeminiGenerateContentSession`。
   - 对 provider 需要的额外 header 做集中处理，例如 OpenRouter 可追加 `HTTP-Referer` / `X-Title`，但不要阻塞基础可用性。

## 测试计划

单元测试：

- provider catalog 排除 `cherryin`。
- provider catalog 只包含可配置 Base URL provider。
- provider 选择能解析正确默认 baseUrl。
- OAuth/CLI/云厂商专有鉴权 provider 不会进入可选 provider 列表。
- OpenAI-compatible、Anthropic、Gemini 的模型列表响应解析、空列表、错误响应、非 JSON 响应。
- 多模态判断优先级：API 能力 > catalog 能力 > 启发式 > text-only。

集成/手动测试：

- OpenAI-compatible provider：OpenAI、DeepSeek、OpenRouter、硅基流动、月之暗面。
- Anthropic Base URL provider：Anthropic 官方或兼容 Anthropic Messages 的中转服务。
- Gemini Base URL provider：Gemini 官方或兼容 Gemini Generate Content 的中转服务。
- 本地 provider：Ollama、LM Studio。
- 无 API Key provider：确认 UI 不强制填写 key。
- 错误路径：无效 key、无效 baseUrl、网络超时、provider 返回 401/404。
- 视觉模型：选择支持图片的模型后附件按钮显示；选择 text-only 模型后附件按钮隐藏。

构建验证：

- `dotnet build TuneLab.sln -c Debug`
- 若只改宿主内部 `TuneLab/Agent` 与 UI，不应触发 `TuneLab.SDK` / `TuneLab.Foundation` PublicAPI 变更。
- 如果新增通用 `ControllerConfig` 到 `TuneLab.SDK`，这属于 SDK public API additive change，必须写入 `PublicAPI.Unshipped.txt`，并明确说明新增 ABI。

## 风险与决策点

- “支持 Cherry Studio provider”在本计划中定义为“支持 Cherry Studio 里可配置 Base URL，且 endpoint family 属于 OpenAI-compatible / Anthropic / Gemini 的 provider”，不是全部 provider。
- 如果为了 API Key 按钮和动态模型列表修改 `TuneLab.SDK` controller config，会触及冻结 ABI。更稳妥的第一版是只在 Agent 设置页手写专用 UI。
- Cherry Studio catalog 更新频繁，手动复制 provider 容易过期。建议引入生成脚本或至少保留来源注释。
- 多模态能力没有统一标准，必须允许用户刷新与容错，并默认保守。
- OAuth/CLI、Azure/AWS/Vertex 这类非 Base URL provider 不应出现在本计划的下拉列表中。

## 建议落地方案

第一版建议实现可配置 Base URL provider：OpenAI-compatible family、Anthropic Messages、Gemini Generate Content、OpenRouter、Ollama/LM Studio、本地服务和自定义聚合服务，完成 provider 预设、自动 baseUrl、模型拉取、多模态判断、API Key 链接。

这样可以尽快解决用户最痛的“地址和模型手填”问题，同时把范围控制在 Base URL + API Key/无 key 的 HTTP provider 内，避免把 OAuth、云厂商签名、service account 等复杂登录模型混进来。
